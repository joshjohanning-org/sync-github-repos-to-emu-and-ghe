repos:
  - source: joshjohanning-org/ghas-demo
    target: joshjohanning-org/ghas-demo
    visibility: internal # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
  - source: joshjohanning-org/.github-private
    target: joshjohanning-org/.github-private
    visibility: private # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
  - source: joshjohanning-org/juice-shop-ghas-workshop
    target: joshjohanning-org/juice-shop-ghas-workshop
    visibility: internal # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
  - source: github-samples/securing-your-code
    target: joshjohanning-org/securing-your-code-workshop
    visibility: internal # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
  - source: joshjohanning-org/reusable-workflows
    target: joshjohanning-org/reusable-workflows
    visibility: internal # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
  - source: joshjohanning-org/required-workflows
    target: joshjohanning-org/required-workflows
    visibility: internal # Optional: private, public, or internal (defaults to private)
    disable-github-actions: false # Optional: disable Actions on target repo (defaults to true)
    archive-after-sync: false # Optional: archive repo after sync (defaults to false)
